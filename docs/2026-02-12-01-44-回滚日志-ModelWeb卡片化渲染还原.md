# 2026-02-12-01-44-回滚日志-ModelWeb卡片化渲染还原

## 背景 (Background)
在之前的开发中，为了提升 Model Web 的“流水线”感，我们引入了基于 LangGraph 节点的卡片式消息渲染协议（每个节点输出一个单独的卡片并支持折叠）。但在实际使用中发现这种方案导致 UI 过于细碎且增加了通信复杂度。

根据用户要求，我们将回滚这部分变更，恢复为简洁的纯文本消息流。

## 回滚内容 (Reverted Changes)

### 1. UI 渲染层 (apps/studio/src/components/assistants/web-architect/thread.tsx)
- **回滚前**: `WebMarkdownText` 负责根据 `metadata.langgraph_node` 渲染带标题、图标和折叠逻辑的卡片。
- **回滚后**: 恢复为标准的文本消息展示。移除了所有卡片外壳和折叠逻辑。

### 2. 后端消息协议 (apps/studio/src/assistants/web-architect/...)
- (待执行) 移除发送 `name="status"` 消息的逻辑。
- (待执行) 停止在元数据中注入 `langgraph_node` 信息。

### 3. 工具调用渲染 (apps/studio/src/components/assistant-ui/tool-call-renderer.tsx)
- (待执行) 简化属性配置。

## 记录与备注 (Notes)
- 保留了 `Model Web` 的多 Runtime 隔离架构。
- 保留了 VFS 的基本交互。

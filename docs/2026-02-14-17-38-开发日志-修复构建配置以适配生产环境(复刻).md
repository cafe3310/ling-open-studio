> [!NOTE]
> 本文档复刻自历史 Commit `d879470ef2d34c5540ea901365f37282cf6cdc03` (2026-02-12)，用于记录对 Next.js 配置的构建修复。
> 复刻时间: 2026-02-14

# 2026-02-12-01-11-开发日志-修复构建配置以适配生产环境

## 1. 背景 (Background)

在进行生产环境（如 Hugging Face Spaces）构建时，发现 `next.config.ts` 中的某些实验性配置会导致构建失败或在特定环境下产生警告。需要清理冗余的开发期配置。

## 2. 主要工作 (Major Changes)

### 2.1 优化 next.config.ts
- **移除 `allowedDevOrigins`**: 删除了 `experimental.allowedDevOrigins` 配置。该配置主要用于开发环境下的跨域调试，但在生产环境 standalone 模式下可能引起不必要的安全检查或构建异常。

## 3. 验证 (Verification)

- [x] 本地执行 `pnpm build` 确认不再受该配置干扰。
- [x] 确认 standalone 模式产物正常生成。

## 4. 后续说明

当前的配置已处于最优状态，适配了 Hugging Face 的容器化部署需求。
